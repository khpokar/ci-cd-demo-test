version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - echo Build started on `date`
      - echo Kishan Pokar First BuildSpec

artifacts:
   files:
     - '**/*'
